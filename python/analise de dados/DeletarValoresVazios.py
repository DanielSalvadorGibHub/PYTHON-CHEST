#todos ou alguns valores em linha:
data = data.dropna(how="all", axis=0)
data = data.dropna(how="any", axis=0)
#todos ou alguns valores em coluna:
data = data.dropna(how="all", axis=0)
data = data.dropna(how="any", axis=0)
display(data)